services:
  peer0-org1:
    image: hyperledger/fabric-peer:2.5
    container_name: peer0.org1.example.com
    env_file:
      - ../../env/org1-peer.env.example
    volumes:
      - /var/run/docker.sock:/host/var/run/docker.sock
      - /srv/fabric/org1-peer/msp:/etc/hyperledger/fabric/msp:ro
      - /srv/fabric/org1-peer/tls:/etc/hyperledger/fabric/tls:ro
      - /srv/fabric/org1-peer/data:/var/hyperledger/production
    ports:
      - "7051:7051"
      - "9443:9443"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "sh", "-c", "nc -z 127.0.0.1 7051"]
      interval: 30s
      timeout: 5s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "10"
